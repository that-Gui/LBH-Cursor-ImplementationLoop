{
  "name": "lbh-engineering-implementation-loop",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare-run": "tsx src/cli.ts prepare",
    "complete-run": "tsx src/cli.ts complete",
    "finalize": "tsx src/cli.ts finalize",
    "test": "tsx --test test/hooks.test.ts test/finalize.test.ts test/cursor-contract.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.6.0"
  }
}
